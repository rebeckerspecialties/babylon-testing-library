describe('text query', () => {
    it('should query a scene for a control with matching text', () => {});

    it('should query a texture for a control with matching text', () => {});

    it('should query a control for a control with matching text', () => {});
});
